# Temporary graphics-freeze diagnosis only: this increases playback CPU usage.
# Remove this drop-in and log out/in to restore automatic hardware decoding.
[Service]
Environment="QT_MEDIA_BACKEND=ffmpeg"
Environment="QT_FFMPEG_DECODING_HW_DEVICE_TYPES=,"
Environment="QT_DISABLE_HW_TEXTURES_CONVERSION=1"
